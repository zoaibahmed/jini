version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: jini-postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: jini_user
      POSTGRES_PASSWORD: jini_secure_pass
      POSTGRES_DB: jini_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: jini-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
